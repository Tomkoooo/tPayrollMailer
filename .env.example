# Database
MONGODB_URI=mongodb://localhost:27017/berlap

# Auth.js
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Microsoft Azure AD (for Auth.js login)
AUTH_AZURE_AD_CLIENT_ID=your-client-id
AUTH_AZURE_AD_CLIENT_SECRET=your-client-secret
AUTH_AZURE_AD_TENANT_ID=your-tenant-id

# Email Provider Selection ('smtp' or 'graph')
EMAIL_PROVIDER=smtp

# SMTP Configuration (if using EMAIL_PROVIDER=smtp)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Microsoft Graph API (if using EMAIL_PROVIDER=graph)
AZURE_CLIENT_ID=your-application-client-id
AZURE_CLIENT_SECRET=your-application-client-secret
AZURE_TENANT_ID=your-tenant-id

# Finance Email (sender address)
FINANCE_EMAIL=penzugy@company.hu
